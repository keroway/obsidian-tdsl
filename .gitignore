node_modules/
main.js
*.js.map
.DS_Store

# Environment / secrets
.env
.env.*
!.env.example
*.secret
*.pem
*.key

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# Pi coding agent — local mesh / agent config (per-developer, not shared)
.pi/
.claude/settings.local.json

# Codex CLI Stop hook config — hooks.json embeds absolute paths, so sharing it
# breaks other machines (keroway/agent-assets#186)
.codex/

# OS
Thumbs.db

# vitest --coverage の出力。生成物なのでコミットしない。
# biome は useIgnoreFile: true でこのファイルを読むため、
# ここに書くことで lint 対象からも外れる（#188）。
coverage/

# Codex CLI / agent-assets symlink (~/.agents) / pi のローカル生成物・個人状態。
# いずれも絶対パスや個人環境固有の値を含みうる個人ローカル層で、
# 共有すると他の環境で壊れる (keroway/agent-assets#186)
.agents/
