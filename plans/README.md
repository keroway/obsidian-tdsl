# Implementation Plans

Generated by the improve skill on 2026-06-28 at commit `6afabb2`.
Execute in the order below. Each executor: read your plan fully before starting,
honor its STOP conditions, and update the Status column when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| ---- | ----- | -------- | ------ | ---------- | ------ |
| [001](./001-extract-settings-parsers-to-utils.md) | Extract settings-coercion helpers to utils.ts + vitest config | P1 | S | — | DONE |
| [002](./002-test-format-current-block.md) | Extract and unit-test the pure logic of formatCurrentBlock | P1 | M | 001 | DONE |
| [003](./003-add-npm-audit-to-ci.md) | Add npm audit to CI | P2 | XS | — | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED <reason>` |
`REJECTED <rationale>`

## Dependency notes

- **002 requires 001**: Plan 001 adds `vitest.config.ts` and the `test:coverage`
  script that Plan 002's maintenance notes reference. Plan 002 also adds new
  exports to `src/utils.ts` — if Plan 001 has already added `parseScaleSetting`
  and `parseLaneHeightSetting`, Step 1 of Plan 002 simply appends after them.
- **003 is independent**: Can be executed in parallel with 001/002.

## Findings considered and rejected

- **`formatCurrentBlock` missing `ensureWasm()` guard**: False alarm. Line 217
  of `src/main.ts` shows `await ensureWasm()` is already called in the
  `editorCallback` before `formatCurrentBlock`. Not a bug.
- **`parseDiagnostics` bare JSON.parse**: Low risk — WASM output is deterministic
  and same-author. Failure propagates to the outer catch with a legible error.
  Not worth a plan.
- **Japanese-only settings UI**: By-design. Primary audience is Japanese-speaking.
  Not a finding.

## Direction finding (not planned — for maintainer consideration)

**`lint_fix_source` UX gap**: The `LintIssue.fixable` flag and the "✏" badge
in lint output imply the WASM exports a `lint_fix_source` function. Currently
no command applies those fixes. A "Fix lint issues in current tdsl block"
palette command (parallel to `format-tdsl-block`) would complete the UX loop.
Effort M. Not planned here because it requires verifying the `lint_fix_source`
WASM API signature and is a feature addition rather than a correctness fix.
